---
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';

import { Background } from '@/components/background';
import { FAQ } from '@/components/blocks/faq';
import { Features } from '@/components/blocks/features';
import { Hero } from '@/components/blocks/hero';
import { Standard } from '@/components/blocks/standard';
import { Pricing } from '@/components/blocks/pricing';
import { Screenshots } from '@/components/blocks/screenshots';
import { ResourceAllocation } from '@/components/blocks/resource-allocation';
import { Testimonials } from '@/components/blocks/testimonials';
import DefaultLayout from '@/layouts/DefaultLayout.astro';

// <Testimonials client:only='react'/>
// <ResourceAllocation />

---

<DefaultLayout title={SITE_TITLE} description={SITE_DESCRIPTION} >
  <Background className="via-muted to-muted/80">
    <Hero />
    <Screenshots client:only='react'/>
    <Standard client:only="react"/> 
    <Features />
  </Background>
  <Background variant="bottom">
    <!-- <Pricing client:only='react'/>-->
    <FAQ client:only='react'/>
  </Background>
</DefaultLayout>